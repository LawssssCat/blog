server:
  port: 28080
spring:
  application:
    name: service-order # nacos 获取该配置作为服务名称
  cloud:
    nacos: # 默认值如下
      server-addr: localhost:8848
      discovery:
        username: nacos
        password: nacos
        # 隔离配置环境，如 dev/pro，默认 public
        namespace: public
